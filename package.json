{
  "name": "solari-reflex",
  "version": "0.1.0",
  "description": "A speed layer for Solari computer use: one structured observation, one Jev decision, one verified action per step.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "NOTICE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && mkdir -p dist/desktop && cp src/desktop/reflexd.py dist/desktop/",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "prepare": "tsc -p tsconfig.build.json && mkdir -p dist/desktop && cp src/desktop/reflexd.py dist/desktop/"
  },
  "engines": {
    "node": ">=22.18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hitakshiA/solari-reflex.git"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
